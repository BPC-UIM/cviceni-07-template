# Cviceni 07 — Rozhodovaci stromy a nahodny les
# Plne zamrazene, prikvacene verze (pip freeze). Instalace: pip install -r requirements.txt
#
# Prime zavislosti: numpy (vypocty), scikit-learn (referencni modely v testech
# a demonstrace boostingu), matplotlib (grafy), PyYAML (config), pytest (testy).
# Strom i les jsou psane od nuly — nic dalsiho neni potreba.
# Zbytek seznamu jsou tranzitivni zavislosti techto peti balicku.
colorama==0.4.6
contourpy==1.3.1
cycler==0.12.1
fonttools==4.61.1
iniconfig==2.3.0
joblib==1.5.3
kiwisolver==1.4.9
matplotlib==3.10.1
numpy==2.4.0
packaging==25.0
pillow==12.1.0
pluggy==1.6.0
Pygments==2.20.0
pyparsing==3.3.1
pytest==9.1.1
python-dateutil==2.9.0.post0
PyYAML==6.0.3
scikit-learn==1.8.0
scipy==1.15.2
six==1.17.0
threadpoolctl==3.6.0
